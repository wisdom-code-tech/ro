# 依赖与构建产物
server/node_modules/
server/dist/
**/*.log

# 运行数据（含下载文件 / SQLite / 音源脚本）
data/downloads/
data/db/
data/sources/
data/*.db
data/*.db-wal
data/*.db-shm

# 真实配置（含密码 / apiKey），改用 config.example.yaml 模板
config.yaml

# 杂项
.DS_Store
